<p>Password reset link: <%= shorten_with_bitly(edit_password_url(@resource, :reset_password_token => @resource.reset_password_token))%></p>
&nbsp;
<p>Someone has requested a link to change your password, and you can do this through the link above.</p>&nbsp;
<p>If you didn't request this, please ignore this message.</p>&nbsp;
<p>Your password won't change until you access the link above and create a new one.</p>

